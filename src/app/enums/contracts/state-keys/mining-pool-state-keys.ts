export enum MiningPoolStateKeys {
  MiningGovernance = 'PW',
  MinedToken = 'PX',
  MiningPeriodEndBlock = 'PY',
  RewardRate = 'PZ',
  MiningDuration = 'PAA',
  LastUpdateBlock = 'PAB',
  StakingToken = 'PN', // same as staking pool state key
  TotalSupply = 'PA' // same as base pool state key
}
